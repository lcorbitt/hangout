<% if user_signed_in? %>
  <%= react_component("MemberHeader", { avatarUrl: current_user.avatar.attached? ? current_user.avatar.service_url : '', userId: current_user.id }) %>
<% else %>
  <%= react_component("PublicHeader") %>
<% end %>
